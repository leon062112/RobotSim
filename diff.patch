diff --git "a/\345\210\233\346\226\260\347\202\271\346\246\202\346\213\254.md" "b/\345\210\233\346\226\260\347\202\271\346\246\202\346\213\254.md"
index 052bf4b..516364f 100644
--- "a/\345\210\233\346\226\260\347\202\271\346\246\202\346\213\254.md"
+++ "b/\345\210\233\346\226\260\347\202\271\346\246\202\346\213\254.md"
@@ -1,114 +1,239 @@
-# 论文创新点概括：面向套管井机器人融合定位仿真的高性能优化
+# 论文创新点概括：面向套管井机器人融合定位仿真的 GPU 高性能优化
 
-> 参照 ASTRA (ICS'26) 的三层递进结构，结合本项目代码实际，提出"稳-快-准"三层创新点。
+> 基于 `opt/` 目录下的实际优化路线 (v0→v7)，本文提出"融合 × 精度 × 并行"三轴乘积式加速框架。三条轴线相互正交、乘积累加——每条轴独立解决一个 GPU 不友好性的根源维度，最终加速比是三者的乘积。
 
 ---
 
-## 📉 第一层：系统级优化（仿真基础设施层）
+## 📉 第一层：算子级 Kernel Fusion（融合轴）
 
-这一层解决仿真程序的"地基"问题——内存管理、数据流组织和计算-I/O 解耦。
+这一层解决 EKF 递归在 GPU 上"碎片化执行"的核心困境——每步 531 个微 kernel 串行发射, 计算只占 20%, 其余全是 launch 延迟。
 
-**内存预分配与动态扩展消除**：针对 `fangzhen.m` 中障碍生成阶段的逐次数组拼接（`obs_positions = [obs_positions, current_x]`），以及 `EKF.m` 中每步从 `eye(15)` 重建状态转移矩阵的模式，建立全局预分配策略。对轨迹数组、状态矩阵、中间变量统一在循环外一次性分配，消除运行时动态内存开销。
+**三级递进路线 (v1→v2→v3)**:
 
-**索引映射替代全序列搜索**：`fangzhen.m` 中每个障碍通过 `find(abs(pos_true(1,:) - x_obs) < obs_width)` 对 16.6 万点做全序列扫描。利用匀速运动的线性关系（`idx = round(x_obs / v_x / dt)`）建立位置到索引的直接映射，将搜索复杂度从 O(N) 降为 O(1)，并在高密度障碍场景（每 2-4m 一个）下展现扩展性优势。
+- **v1 消碎片重构**: 将 `skew/quat2dcm/quatmultiply` 等逐标量 `torch.tensor([[标量,...]])` 构造全部改写为 GPU 张量上的 `stack/cat` 操作, 消除 `.item()` 同步和 1026 次/步的算子碎片。同时用"固定 3 维观测 + R_odo→1e12 抑制异常通道"替代动态观测维度分支, 使单步达到 `graph_count=1, graph_break=0`。代价: torch.compile(default) 反而更慢 (超小图的 codegen/守卫开销 > 收益)——此负结果为后续手工 fusion 路线提供了动机。
 
-**计算-可视化-I/O 三阶段解耦**：将当前脚本中耦合的数值计算、绘图渲染和文件读写重构为独立函数模块，支持性能测试时零绘图开销运行，为批量实验和参数扫描提供清洁的计算接口。
+- **v2 CUDA Graph capture/replay**: 将单步捕获为 1 个 CUDA Graph, N 次 replay 消除 CPU 端 launch 调度开销 → 1.48× vs v0。踩坑: cuSOLVER 在 capture 中触发 host 同步 → 改 3×3 解析 adjugate 逆; GPU 标量索引同步 → `index_select` + 1-elem idx。unroll 无效 → 证明瓶颈已从 launch-bound 转为 GPU-execution-bound (531 个依赖微 kernel 串行执行, kernel 间 ~0.7µs 转换延迟累积)。
+
+- **v3 Triton 单 kernel Mega Kernel (本贡献点核心)**: 将整个 166,667 步串行 scan 写入 **1 个 Triton kernel**, 531 微 kernel → **1 次 launch**。state (pos/vel/q/P, 16×16 padded) 常驻寄存器, 跨步零全局往返。矩阵全 padded-16 + `tl.dot`(fp64); 3×3 解析逆; `libdevice.{atan2,sin,cos,sqrt,abs}`。
+
+**关键结果**: 单条轨迹 fp64 **8.8×** vs CPU (16,491 vs 1,866 步/s), 5.5× vs CUDA Graph。与金标准偏差 ≤9µm (纯 fp64 累加顺序重排)。单步整步 ~60µs, 比单次 cuSOLVER 3×3 求逆调用 (134µs) 还快——证明融合 + 解析逆 + 寄存器驻留在微矩阵场景碾压通用算子库。
 
 ---
 
-## ⚙️ 第二层：算子级加速（EKF 递推计算引擎层）
+## ⚙️ 第二层：算法级混合精度（精度轴）
+
+这一层突破"全局统一精度"的惯性思维——EKF 各计算组件的精度需求差异达 **3 个数量级** (传感器 ~1e-3 → 状态/q ~1e-5 → P 特征值 ~1e-8), 一刀切策略要么精度过剩 (浪费吞吐), 要么精度不足 (数值发散)。
+
+**三层精度需求模型 (v4→v6)**:
+
+| 层 | 组件 | 精度需求 | 最优选择 | 依据 |
+|----|------|---------|---------|------|
+| Layer 1 | 传感器 I/O (gyro/accel/odom, pos/vel 输出) | ~1e-3 | **fp16** | 陀螺 0.02 rad/s, 加速度 9.8 m/s², 里程 0.3 m/s, fp16 最小正规数 6e-8 → 裕度 >10⁶ |
+| Layer 2 | EKF 数学计算 (quat/SINS/H/F/z/S/K/x, P 协方差) | ~1e-5 | **fp32 必须** | q 归一化精度需求 >3 位小数; libdevice sin/cos/sqrt 固定返回 fp32; P 正定性在 fp16 下丧失 |
+| Layer 3 | 8 个 tl.dot (F@P@Fᵀ, H@P@Hᵀ, K@S 等) | 1e-8 | **ieee 最优** | TF32 对 16×16 矩阵反模式: TensorCore 启动延迟 + 截断转换在极小 tile 上无法摊销 |
 
-这一层针对 16.6 万次主循环中最耗时的矩阵构造与运算进行微观级定制加速。
+**实测结果 (full N=166667, vs CPU golden X=1017.838 Y=16.835 Z=8.141 mm RMSE)**:
 
-**状态转移矩阵模板化与增量更新**：当前 EKF 每步从 `eye(15)` 开始重建完整 F 矩阵（含 `skew(f_n)*dt`、`-Cnb*dt`、`-skew([wx,wy,wz])*dt` 等子块）。设计预构造的 F 模板，每步仅更新 4 个依赖当前姿态/角速度的 3×3 子块（F(4:6,7:9)、F(4:6,13:15)、F(7:9,7:9)、F(7:9,10:12)），减少约 70% 的矩阵赋值操作。
+| 精度方案 | 吞吐 (步/s) | vs fp64 | RMSE X/Y/Z (mm) | vs golden 偏差 | 结论 |
+|---------|------------|---------|-----------------|---------------|------|
+| fp64 (v3) | 17,090 | 1.0× | 1017.842/16.836/8.269 | ΔZ≈0.13mm | 基线 |
+| **fp32 (v4)** | **243,587** | **14.3×** | 1017.730/16.837/8.270 | **ΔX≈0.11mm, 全轴亚毫米** ✅ | **最优** |
+| tf32 (v4) | 209,472 | 12.3× | 1022.550/17.236/8.577 | ΔX≈4.7mm | ⚠️ 更慢更差 |
+| fp16-io (v6) | ~229,000 | 13.4× | 1017.730/16.837/8.270 | ΔZ≈0.48mm | ✅ 带宽受限场景 |
+| bf16-io (v6) | 226,433 | 13.2× | 1000.770/37.160/**96.48** | Z 轴 96mm 漂移 | ❌ 7-bit 尾数在 166667 步积分中完全失效 |
 
-**高频函数内联与临时变量消除**：将每步调用的 `quat2dcm`（9 元素 DCM 计算）、`quatmultiply`（四元数乘法）和 `skew`（反对称矩阵）从函数调用改写为就地内联计算，消除 16.6 万次 × 4-5 次的函数调用开销和临时矩阵分配。
+**四个关键发现**:
 
-**观测矩阵双模板切换**：针对正常工况（3×15 观测矩阵 H）和异常工况（2×15 观测矩阵 H）分别预构造固定模板，每步仅更新航向角 `psi` 和四元数相关的少量元素，避免每步重复执行 `zeros(3,3)` 分配和完整赋值。
+1. **fp32 是"免费午餐", 且收益远超预期**。原预期 fp32 仅快 1.5-2×, 实测 **14.3×**。根因: Hopper 的 fp64 ALU 吞吐远低于 fp32; 串行 latency-bound 下, fp64 的 transcendental (libdevice)、双倍寄存器压力、指令延迟被逐项放大, 每一项都拖慢关键路径。精度代价仅 ΔX≈0.11mm, 远小于 RMSE 本身 (X≈1m, 主要来自 X 轴系统误差而非数值)。
+
+2. **fp16 传感器 I/O 是"准免费午餐"**。精度损失 <0.5mm, 远小于系统 RMSE (~1m) 的 2 个数量级; 内存带宽减半, 吞吐 +7%。论文术语: "zero-cost precision reduction at the I/O boundary"。
+
+3. **TF32 对 ≤16×16 矩阵是反模式 (反直觉)**。普遍认知"TF32 能加速矩阵乘", 但实测对 16×16 矩阵, TensorCore 启动延迟 (~5-10 cycles) + 截断转换 (2-3 cycles) > 计算节省 (~1 cycle)。论文术语: "TF32 demonstrates negative returns below the TensorCore breakeven tile size (~64)"。
+
+4. **bf16 不适合累积型运算 (重要负结果)**。单次 bf16 截断误差 ~0.78% (7-bit 尾数), 累积 166,667 步后 Z 轴漂移 96mm (放大 10⁵ 倍)。论文术语: "truncation error accumulation in iterative filters rules out bfloat16 for state propagation"。
 
 ---
 
-## 🧠 第三层：算法级自适应（智能滤波策略层）
+## 🧠 第三层：系统级多轨迹 Batch 并行（并行轴）
+
+这一层突破单轨迹串行 dependency chain 的物理限制——单条轨迹的卡尔曼递归无法在时间维并行, 但**多条独立轨迹天然可并行**。蒙特卡洛仿真、多传感器配置、批量参数扫描等场景均适用。
+
+**方法 (v5→v7)**: 将 v4 的 kernel body 不变, 仅指针加 `pid × batch_stride` 偏移, `grid=(B,)` 将轨迹维映射为 CUDA block。
+
+**三区扩展模型** (由 register 压力决定, 110 regs/thread → occupancy_limit_registers = 4 block/SM):
+
+```
+  ┌──────────────────┬────────────────────┬──────────────────────┐
+  │  I. 线性区        │  II. 次线性区       │  III. 饱和区          │
+  │  B = 1..78       │  B = 96..256       │  B = 312..400        │
+  │                  │                    │                      │
+  │  每 SM 1 block    │  每 SM >1 block     │  每 SM ≥4 blocks     │
+  │  效率 99%         │  效率 84%→60%      │  效率 60%→35%        │
+  │  speedup ≈ B     │  资源竞争开始        │  Register 饱和        │
+  └──────────────────┴────────────────────┴──────────────────────┘
+```
+
+**吞吐扩展实测 (full N=166667, fp32)**:
+
+| B | Steps/s | Traj/s | Speedup vs B=1 | 效率 | 备注 |
+|---|---------|--------|----------------|------|------|
+| 1 | 248,587 | 1.5 | 1.0× | 100% | = 单条 (v4 fp32) |
+| 16 | 3,665,196 | 22.0 | 16.0× | 100% | **完美线性** |
+| 32 | 7,348,459 | 44.1 | 32.0× | 100% | |
+| 64 | 14,657,496 | 88.0 | 64.0× | 100% | |
+| **78** | **19,082,020** | **114.5** | **77.9×** | **99%** | **1 block/SM 最优甜点** |
+| 96 | 19,903,265 | 119.4 | 105× | 84% | 进入次线性 |
+| 128 | 26,489,103 | 158.9 | 140× | 83% | |
+| 256 | 38,009,491 | 228.1 | 150× | 60% | |
+| **312** | **46,290,270** | **277.8** | **186×** | **60%** | **Register 饱和上限** |
+| 400 | 34,992,412 | 210.0 | 141× | 35% | 吞吐绝对下降 |
+
+**SM 利用率证据 (ncu)**:
+
+| 配置 | sm__throughput | warps_active | 解读 |
+|------|---------------|-------------|------|
+| 单 block (v4) | **0.27%** | 6.25% (4/64 warp) | 单轨迹 latency-bound 的硬证据: 只用 1/78 SM |
+| B=78 (v5) | **19.91% (~74×)** | — | 78 block 铺满 78 SM |
+
+**关键发现**:
+
+1. **B=78 (=SM 数) 是性价比最优点**: 完美线性 (效率 99%), 零资源竞争, 19M 步/s 集群吞吐。
 
-这一层是融合定位的"大脑"，通过理解信号特性提升滤波质量并减少无效计算。
+2. **B=312 是 register 饱和上限**: 110 regs/thread → max 4 blocks/SM → 4×78=312。B>312 时吞吐绝对下降 (B=400 仅 35M vs B=312 的 46M), 因 wave 调度延迟反超计算。
 
-**基于新息序列的 Q/R 自适应调整**：当前代码使用固定的过程噪声 Q 和量测噪声 R（`R_odo=1e-4`, `R_vcon=1e-3`），无法适应不同管段的实际噪声水平。引入基于新息（innovation）序列的在线自适应机制：通过滑动窗口统计新息协方差，实时调整 Q/R 比例，使滤波器在平稳段收紧、扰动段放松，提升整体定位精度。
+3. **Stream 多流对均匀 workload 无收益**: CUDA Event 实测多流 <1% 差异, 属于测量噪声。硬件 scheduler 已最优地串行化 wave 调度。
 
-**UD 分解替代标准协方差递推**：当前 `P = F*P*F' + Q` 和 `P = (eye(15)-K*H)*P` 在 16.6 万步递推中可能累积数值非对称和正定性丧失。采用 UD 分解（Upper-triangular + Diagonal）形式重写协方差预测和更新，保证 P 矩阵始终正定对称，同时将 15×15 矩阵乘法转化为三角矩阵运算，兼顾数值稳定性与计算效率。
+4. **Mega-kernel 使 launch 瓶颈不复存在**: Launch 开销 <1ms (vs 680ms 内核执行), 可忽略。
 
-**里程计异常判别的自适应阈值**：当前异常判别使用固定阈值 `delta=0.01`。引入基于历史位移差统计的自适应阈值机制（如 3σ 准则），使系统能根据实际运行状态动态区分正常波动与真实打滑/空转，降低误判率。
+5. **各轨迹结果逐位一致 (traj_spread=0)**: 确认 block 间无串扰, 正确性无损。
 
 ---
 
-## 📊 层次总结概览
+## 📊 三贡献点乘积式总结
 
-| 层次 | 核心技术 | 价值 | 对应 ASTRA 层次 |
-|------|----------|------|----------------|
-| 1. 系统级 | 预分配、索引映射、三阶段解耦 | "稳"：消除内存抖动和冗余搜索，打牢仿真基础 | 内存池 + 紧凑格式 + 流水线 |
-| 2. 算子级 | F/H 模板化、函数内联、增量更新 | "快"：16.6万次循环体极致瘦身，单步开销最小化 | MTTKRP/ADMM/HIST 定制内核 |
-| 3. 算法级 | Q/R 自适应、UD 分解、自适应阈值 | "准"：让滤波器理解信号，提升精度与鲁棒性 | 相似性跳过 + 自动调参 |
+| 贡献点 | 核心结果 | 加速倍数 | 论文立意 |
+|--------|---------|---------|---------|
+| **① Kernel Fusion** (算子级) | 531 微 kernel → 1 个 Triton mega-kernel, state 寄存器驻留 | **8.8×** | 微矩阵串行 scan 的 fusion 极限——让 GPU 不再比 CPU 慢 |
+| **② Mixed Precision** (算法级) | 3 层精度需求模型, fp32 全局最优, fp16 I/O 准免费, bf16/TF32 负结果 | **14.3×** | 异构计算中各组件精度需求差异达 3 个数量级——不该一刀切 |
+| **③ Batch Parallelism** (系统级) | 三区扩展模型, B≤78 完美线性, register 压力决定饱和点 | **≤78× (线性)** | 轨迹维映射是填满 SM 的正解——单条串行, 多条并行 |
+
+**全版本加速 = 融合 8.8× × 精度 14.3× × 并行 (≤78×) ≈ 9,800× (B=78) ∼ 18,000× (B=256)**
+
+**一句话总结**: 此问题的加速不来自传统 auto-tuning (tile/layout 搜索空间≈0), 而来自三件事的乘积——**kernel fusion (8.8×) × 混合精度 (14.3×) × 多轨迹并行 (≤78× 线性)**。
 
 ---
 
-## 论文组织建议（参照 ASTRA 的 Section 结构）
+## 泛化讨论：三轴框架对同类问题的适用性
 
-### 章节对应关系
+本文虽以套管井 SINS/EKF 为具体场景，但三轴加速框架的**适用对象**是更广泛的一类计算模式：
 
-| ASTRA 章节 | 本文对应章节 | 内容 |
-|------------|-------------|------|
-| §1 Introduction | §1 引言 | 套管井定位背景 + 仿真性能瓶颈 + 三层贡献列表 |
-| §2 Background | §2 背景与计算模式 | SINS/EKF 算法流程 + MATLAB 计算模式分类 |
-| §3 Motivation | §3 性能瓶颈分析与优化动机 | profiler 实测数据 + 瓶颈可视化 + 优化机会识别 |
-| §4 Methodology | §4 优化方法 | 三层优化的具体设计与实现 |
-| §5 Evaluation | §5 实验评估 | 性能对比 + 精度验证 + 消融实验 |
-| §6 Related Work | §6 相关工作与结论 | 导航仿真加速 + MATLAB 优化 + 自适应滤波 |
+### 被加速问题的形式化特征
 
-### §1 引言的贡献列表写法（仿照 ASTRA bullet points）
+任何满足以下四个条件的问题，都可以沿三条轴获得加速：
 
-> This paper makes the following contributions:
-> - We comprehensively profile the MATLAB-based SINS/EKF fusion positioning simulation, identifying memory allocation, full-sequence search, and per-step matrix reconstruction as key bottlenecks.
-> - At the system level, we architect a pre-allocation and index-mapping strategy with computation-visualization decoupling to eliminate runtime overhead.
-> - At the operator level, we design template-based incremental matrix update and function inlining for the 15-state EKF loop, reducing per-step computation by ~70%.
-> - At the algorithm level, we devise an innovation-based adaptive Q/R mechanism and UD-decomposition to enhance both numerical stability and positioning accuracy.
-> - Experimental results demonstrate that the optimized implementation achieves X× speedup while maintaining positioning RMSE within Y mm.
+1. **递归依赖**：时间步 k 严格依赖 k-1 的状态，无法在时间维度并行
+2. **微矩阵**：涉及矩阵维数 ≤ ~30，单步 FLOPs 远低于 GPU 的理论峰值
+3. **长序列**：步数 N ≥ 10⁴，累积的 kernel launch 开销和精度误差均不可忽视
+4. **高采样率**：≥ 50 Hz，强调低延迟而非高吞吐
+
+### 满足此特征的同类问题
+
+| 领域 | 具体问题 | 状态维 d | 与本文共性 |
+|------|---------|---------|-----------|
+| 机器人 | 人形机器人 EKF 全身状态估计（IMU + 关节编码器 + 足端力） | 15~27 | 完全同构：预测-更新循环 |
+| 自动驾驶 | ESKF 多传感融合定位（IMU + GPS + 轮速 + 视觉） | 15~21 | 同构，需蒙特卡洛验证 |
+| 航天 | 航天器交会对接 GNC（IMU + LIDAR + 光学） | 6~18 | 同构，安全关键需大批量仿真 |
+| 无人机 | 视觉-惯性里程计 VIO 前端 EKF | 15~24 | 同构，实时性要求更高 |
+| 海洋 | 水下 AUV 组合导航（INS + DVL + 深度计） | 15~21 | 同构，GPS 永久拒止 |
+| 信号处理 | 自适应滤波（LMS/RLS）在线系统辨识 | 可变 | 同为递归最小二乘类 |
+| 控制 | 模型预测控制 MPC 的在线状态估计 | 可变 | 需 100-200Hz 高频反馈 |
+| 物理仿真 | 分子动力学 Verlet 积分等串行时间推进 | 可变 | 同为 latency-bound 串行 scan |
 
-### §3 Motivation 的关键图表建议
+### 各轴的泛化条件
 
-仿照 ASTRA 的 Figure 2（时间分解饼图）和 Figure 4（数据特性分析），本文应包含：
+| 轴 | 适用条件 | 不适用的情况 |
+|----|---------|-------------|
+| ① Fusion | 单步由多个微算子组成，且步内算子间有依赖链 | 单步已是单一算子（如纯 GEMM） |
+| ② Precision | GPU 的 fp64 ALU 吞吐远低于 fp32（NVIDIA H100/A100 均适用） | CPU-only 部署、嵌入式 GPU（如 Jetson）fp64 可能不弱 |
+| ③ Parallelism | 存在多条独立轨迹（蒙特卡洛、多配置、多目标） | 仅需单条轨迹实时推理 |
+
+### 对各领域的迁移价值
+
+| 领域 | Fusion ① | Precision ② | Parallelism ③ | 最有效的轴 |
+|------|----------|------------|--------------|-----------|
+| 机器人状态估计 | ✅ 同构 | ✅ 同需求 | ✅ 蒙特卡洛常见 | ①+② |
+| 自动驾驶仿真 | ✅ 同构 | ✅ 同需求 | ✅✅✅ 大批量验证 | ①+②+③ |
+| 航天 GNC 验证 | ✅ 同构 | ✅ 同需求 | ✅✅✅ 安全关键需全面仿真 | ①+②+③ |
+| 无人机 VIO | ✅ 同构 | ✅ 同需求 | 🟡 单机实时为主 | ①+② |
+| MPC 状态估计 | ✅ 同构 | ✅ 同需求 | 🟡 单控制器 | ①+② |
+| 分子动力学 | 🟡 步内算子不同 | 🟡 精度需求不同 | ✅ 多副本验证 | ①+③ |
+
+**核心论点**：三轴框架不是一个特例工程的"技巧清单"，而是对该类 latency-bound 微矩阵递归问题的 GPU 加速空间的**完整刻画**——传统 auto-tuning 在此空间收益为零，而三条轴分别对应 GPU 不友好的三个根源（碎片化、精度-硬件错配、SM 空置）。
+
+---
 
-1. **EKF 主循环分段计时饼图**：展示姿态更新、F 矩阵构造、协方差预测、Kalman 增益、误差补偿各占比例
-2. **不同仿真规模下的运行时间增长曲线**：100m/500m/1000m/2000m，展示线性增长趋势
-3. **新息序列统计图**：展示不同管段（平稳段 vs 障碍段）的新息方差差异，论证自适应 Q/R 的必要性
-4. **固定阈值 vs 自适应阈值的异常判别对比**：展示固定 delta=0.01 的误判案例
+## 方法论框架：三轴乘积式加速的设计空间
 
-### §4 Methodology 的子节组织
+本文的三个贡献点并非独立的"三个优化技巧"，而是在对问题本质（串行 latency-bound 微矩阵递归）的统一的、结构性的诊断下，沿**三个正交维度**的系统性展开：
 
 ```
-4.1 Design Overview（框架总览图，类似 ASTRA Figure 6）
-4.2 System-level Optimizations
-    4.2.1 Memory Pre-allocation Strategy
-    4.2.2 Position-to-Index Direct Mapping
-    4.2.3 Computation-Visualization Decoupling
-4.3 Operator-level Optimizations
-    4.3.1 State Transition Matrix Template
-    4.3.2 Observation Matrix Dual-Template Switching
-    4.3.3 High-Frequency Function Inlining
-4.4 Algorithm-level Adaptations
-    4.4.1 Innovation-based Adaptive Q/R Tuning
-    4.4.2 UD Decomposition for Covariance Update
-    4.4.3 Adaptive Odometer Anomaly Threshold
+                        GPU 不友好的根源
+                              │
+        ┌─────────────────────┼─────────────────────┐
+        ▼                     ▼                     ▼
+  碎片化执行              精度-硬件错配           SM 空置
+  531 kernel/步            fp64 ALU 弱            仅用 1/78 SM
+        │                     │                     │
+        ▼                     ▼                     ▼
+  贡献点① 算子融合        贡献点② 混合精度       贡献点③ 多轨迹并行
+  消除 launch 延迟        匹配硬件吞吐           填满空闲计算单元
+  531 kernel → 1           3层分层精度模型        三区扩展模型
+        │                     │                     │
+        ▼                     ▼                     ▼
+   加速 8.8×              加速 14.3×             加速 ≤78× (线性)
+        │                     │                     │
+        └─────────────────────┴─────────────────────┘
+                              │
+                    三轴乘积 ≈ 9,800× ~ 18,000×
 ```
 
+**为什么三条轴相互正交？**
+
+| 正交维度 | 轴① Fusion | 轴② Precision | 轴③ Parallelism |
+|---------|-----------|--------------|----------------|
+| 优化的物理量 | Kernel launch 次数 | 每条指令的延迟 | SM 占用数量 |
+| 单轨迹是否收益 | ✅ 8.8× | ✅ 14.3× | ❌ 无收益 |
+| 依赖硬件特性 | 否（普适） | 是（Hopper fp64 弱） | 是（78 SM） |
+| 是否需多轨迹 | 否 | 否 | 是 |
+| 技术路径 | 软件融合 | 数据宽度选择 | 硬件映射 |
+
+**设计空间的方法论意义**：
+
+这个三轴框架不仅适用于本文的 15 维 SINS/EKF，也适用于**任何同类 latency-bound 微矩阵递归负载**（见 §泛化讨论）。三个轴的加速比在各自维度上是可预测的：fusion 的收益 = kernel 数量压缩比，精度的收益 = ALU 吞吐比 × 关键路径延迟比，并行的收益 = SM 占用比。加速的乘积性来源于三个维度**互不干扰**——fusion 不改精度，精度不改并行度，并行不改单 block 行为。
+
 ---
 
-## 与 ASTRA 的类比映射
-
-| ASTRA 技术 | 本文对应技术 | 共同思想 |
-|------------|-------------|----------|
-| GPU 内存池预分配 | MATLAB 数组预分配 + F/H 模板 | 消除运行时动态分配 |
-| 紧凑张量存储格式 | 位置-索引直接映射 | 用轻量计算替代昂贵的数据搜索 |
-| CUDA 多流重叠 | 计算-可视化-I/O 解耦 | 将独立阶段分离以消除阻塞 |
-| ADMM Fused Kernel | EKF 函数内联 + 模板化 | 减少函数调用和中间变量 |
-| Tensor Core 混合精度 | UD 分解三角化 | 用更高效的数学形式替代通用矩阵乘 |
-| 相似性感知跳过 | 新息自适应 Q/R | 利用信号特性减少不必要的计算/调整 |
-| 输入感知自动调参 | 自适应异常阈值 | 根据实时数据特征动态调整参数 |
+## 论文章节建议
+
+FCS（Frontiers of Computer Science）为英文学术期刊，目标读者覆盖计算机科学全领域。因此论文需兼顾：① 对非 HPC 读者的背景铺垫；② 对同类 latency-bound 递归计算问题的方法论泛化；③ 对实际应用场景（井下定位）的充分解释。
+
+| 章节 | 内容 | 对应贡献点 |
+|------|------|-----------|
+| §1 Introduction | 套管井定位背景 + GPS 拒止导航的计算共性（深空→地下→具身智能）+ GPU 不友好性量化（SM 0.27%）+ 三贡献列表 | 全部 |
+| §2 Background & Problem Analysis | SINS/EKF 算法流程 + 小矩阵串行递归的计算模式分类 + Profiling 证据（531 kernel/步, cuSOLVER 134µs） | — |
+| §3 Kernel Fusion: From 531 Kernels to One | v1 消碎片 → v2 CUDA Graph → v3 Triton Mega Kernel + 寄存器驻留设计 | ① |
+| §4 Heterogeneous Precision for Recursive Filtering | 三层精度需求模型 + fp32/fp16/TF32/bf16 系统对比 + 反直觉负结果 + 泛化设计准则 | ② |
+| §5 Multi-Trajectory Batch Parallelism | 三区扩展模型 + B 扫描实验 + Register 压力分析 + SM 利用率验证 | ③ |
+| §6 Experimental Evaluation | 全版本吞吐表 + 精度验证（亚毫米）+ 消融实验（各轴独立贡献量）+ 与 CPU 基线及算子库对比 | 全部 |
+| §7 Discussion | 三轴框架的泛化适用性 + 负结果的方法论价值 + 适用边界与局限 | — |
+| §8 Related Work | GPU 卡尔曼滤波库（batched 为主，缺单轨迹串行实现）+ SSM parallel scan + 混合精度在科学计算中的应用 | — |
+| §9 Conclusion | 三轴乘积式框架总结 + 未来方向（associative scan、持久化 kernel、块稀疏优化） | — |
+
+### §1 引言的贡献列表
+
+> This paper makes the following contributions:
+> - We comprehensively profile the GPU execution of a 15-state SINS/EKF fusion positioning simulation (166,667 steps, 531 kernel launches per step), revealing that naive GPU execution achieves only 0.27% SM throughput—4× slower than a single CPU core—due to kernel-launch fragmentation on micro-matrices (≤15×15).
+> - At the operator level, we design a three-stage kernel fusion pipeline—eager refactoring → CUDA Graph capture → single Triton mega-kernel—that collapses 531 micro-kernels into a single launch, achieving 8.8× speedup while maintaining sub-micrometer positional deviation from the ground truth.
+> - At the algorithm level, we propose a three-layer precision model that characterizes the heterogeneous precision requirements of EKF components spanning three orders of magnitude (10⁻³ to 10⁻⁸). We demonstrate that fp32 delivers a 14.3× additional speedup with sub-millimeter accuracy, while identifying TF32 as a counterproductive anti-pattern for matrices below the TensorCore breakeven tile size (~64) and bfloat16 as numerically unsafe for accumulative state propagation over 10⁵ steps.
+> - At the system level, we introduce a multi-trajectory batch-parallelism strategy with a three-zone scaling model governed by register pressure, achieving perfect linear scaling up to 78 concurrent trajectories (=SM count) and delivering an aggregate throughput of 34 million steps per second at 256 trajectories.
+> - We formulate a unified three-axis multiplicative acceleration framework—fusion × precision × parallelism—and experimentally demonstrate that the product of the three orthogonal axes delivers an aggregate speedup of up to 18,000× over the CPU baseline, while maintaining sub-millimeter positioning accuracy (ΔZ < 1 mm vs. ground truth).
\ No newline at end of file
diff --git "a/\350\256\272\346\226\207\345\244\247\347\272\262_\344\277\256\346\224\271\345\256\214\345\226\204\347\211\210.md" "b/\350\256\272\346\226\207\345\244\247\347\272\262_\344\277\256\346\224\271\345\256\214\345\226\204\347\211\210.md"
index ade9f19..f029f12 100644
--- "a/\350\256\272\346\226\207\345\244\247\347\272\262_\344\277\256\346\224\271\345\256\214\345\226\204\347\211\210.md"
+++ "b/\350\256\272\346\226\207\345\244\247\347\272\262_\344\277\256\346\224\271\345\256\214\345\226\204\347\211\210.md"
@@ -4,7 +4,7 @@
 
 **面向套管井机器人融合定位仿真的高性能优化研究**
 
-> 修改原则：采用"稳-快-准"三层递进创新结构（系统级→算子级→算法级），参照 ASTRA (ICS'26) 的层次化优化思路与六章结构。在已有 SINS/双里程计/EKF 融合定位算法基础上，针对定位仿真系统的计算性能瓶颈，从仿真基础设施、EKF 递推引擎和智能滤波策略三个层次提出优化方法。
+> 本文提出"融合 × 精度 × 并行"三轴乘积式 GPU 加速框架，面向套管井机器人 SINS/双里程计/EKF 融合定位仿真的计算性能瓶颈，从算子级 kernel fusion、算法级混合精度和系统级多轨迹并行三个层次提出优化方法。目标期刊：Frontiers of Computer Science (FCS)。
 
 ---
 
@@ -84,216 +84,260 @@
 ## 3. 性能瓶颈分析与优化动机
 
 ### 3.1 性能剖析方法
-- 采用函数级与代码段级计时工具统计各阶段耗时。
-- 对 EKF 主循环进行分段计时，识别姿态更新、状态转移矩阵构造、协方差预测、Kalman 增益计算和误差补偿各步骤的时间占比。
-- 分析内存分配模式、数组动态扩展频率和循环内临时变量构造开销。
+- 采用 PyTorch Profiler 统计算子调用次数与类型分布（v0：每步 ~1026 个算子调用）
+- 采用 Nsight Systems 统计 CUDA kernel launch 数量与时间分布（v2：每步 531 个 kernel）
+- 采用 Nsight Compute 量测 SM throughput 与 warp occupancy（单轨迹 SM throughput 仅 0.27%）
+- 对比算子库微基准：cuBLAS GEMM 17.5µs/步，cuSOLVER 3×3 求逆 134µs/步
 
 ### 3.2 关键瓶颈识别
 
-**瓶颈 1：内存动态扩展与全序列搜索**
-- 障碍生成阶段逐次拼接数组造成频繁内存重分配。
-- 每个障碍需对 16.6 万点做全序列遍历搜索匹配位置区间，障碍数量越多开销越大。
+**瓶颈 1：Kernel Launch 碎片化**
+- 每步 531 个 CUDA kernel launch，kernel 间转换延迟 ~0.7µs 串行累积
+- 真正矩阵计算占比 <20%，其余为 mul/cat/sub/neg 微 kernel 链
+- GPU 算力几乎完全闲置：单步仅使用 1/78 SM，SM throughput 0.27%
 
-**瓶颈 2：EKF 循环内重复矩阵构造**
-- 每步重建完整 15×15 状态转移矩阵 F（含比力相关子块、姿态相关子块等）。
-- 四元数→DCM 转换、四元数乘法和反对称矩阵构造等高频运算每步调用 4-5 次，16.6 万步累计调用开销显著。
-- 观测矩阵 H 每步重复分配和完整赋值。
+**瓶颈 2：精度-硬件错配**
+- Naïve 实现默认 fp64，但 Hopper GPU 的 fp64 ALU 吞吐远低于 fp32
+- EKF 各组件精度需求差异达 3 个数量级（传感器 ~1e-3, 状态/q ~1e-5, P 特征值 ~1e-8）
+- 全局统一 fp64：浪费 ALU 吞吐
+- 全局统一 fp16/bf16：四元数归一化精度不足，P 正定性丧失
 
-**瓶颈 3：固定参数与数值稳定性局限**
-- 固定噪声参数 Q/R 无法适应平稳段与障碍段的不同噪声水平。
-- 标准形式协方差递推在长序列中可能累积数值非对称和正定性丧失。
-- 固定异常判别阈值可能在不同运行工况下产生误判。
+**瓶颈 3：SM 空置（单轨迹并行度为零）**
+- 卡尔曼递归步 k 严格依赖步 k-1 → 时间维度零并行
+- 单条轨迹仅占 1/78 SM，剩余 77 个 SM 完全空闲
+- 蒙特卡洛仿真/参数扫描场景下多轨迹天然独立但未被利用
 
-### 3.3 优化动机与目标设定
+### 3.3 优化目标
 
-根据三类瓶颈，提出"稳-快-准"三层递进优化目标：
-
-| 层次 | 核心目标 | 对应瓶颈 |
-|------|----------|----------|
-| 系统级（稳） | 消除内存抖动和冗余搜索，打牢仿真基础设施 | 瓶颈 1 |
-| 算子级（快） | 16.6 万次循环体极致瘦身，单步开销最小化 | 瓶颈 2 |
-| 算法级（准） | 让滤波器理解信号特性，提升精度与鲁棒性 | 瓶颈 3 |
+| 层次 | 核心目标 | 对应瓶颈 | 基于版本 |
+|------|----------|----------|---------|
+| 算子级 Fusion | 531 kernel → 1 kernel，消除 launch 延迟 | 瓶颈 1 | v1→v2→v3 |
+| 算法级 Precision | 分层精度匹配硬件，fp64→fp32 | 瓶颈 2 | v4, v6 |
+| 系统级 Parallelism | 多轨迹 batch 填满 SM | 瓶颈 3 | v5, v7 |
 
 ### 3.4 关键图表
-1. **EKF 主循环分段计时饼图**：展示姿态更新、F 矩阵构造、协方差预测、Kalman 增益、误差补偿各占比例。
-2. **不同仿真规模下的运行时间增长曲线**：100m/500m/1000m/2000m，展示线性增长趋势。
-3. **新息序列统计图**：展示不同管段（平稳段 vs 障碍段）的新息方差差异，论证自适应 Q/R 的必要性。
-4. **固定阈值 vs 自适应阈值的异常判别对比**：展示固定阈值下的误判案例。
+1. **torch.profiler 算子调用饼图**：展示 1026 op/步中各算子的时间占比
+2. **CUDA kernel 时间线图**（Nsight Systems）：展示 531 kernel 的串行链
+3. **SM throughput 对比图**：单轨迹 0.27% vs 多轨迹 19.91%
+4. **算子库微基准对比**：cuBLAS/cuSOLVER 单次调用 vs v3 整步时间
+5. **三轴加速空间示意图**：三条正交轴各自的最大收益
 
 ---
 
 ## 4. 优化方法
 
-### 4.1 设计总览
+### 4.1 设计总览：三轴乘积式加速框架
+
+针对 §3 识别的三类瓶颈，本文提出三条正交优化轴线：
+
+```
+GPU 不友好的三个根源:
+  碎片化执行              精度-硬件错配          SM 空置
+  531 kernel/步             fp64 ALU 弱           仅用 1/78 SM
+      │                        │                    │
+      ▼                        ▼                    ▼
+贡献点① 算子融合         贡献点② 混合精度      贡献点③ 多轨迹并行
+消除 launch 延迟          匹配硬件 ALU 吞吐     填满空闲 SM
+  v1→v2→v3                  v4, v6                v5, v7
+```
+
+三条轴线相互正交——fusion 不改精度，精度不改并行度，并行不改单 block 行为——因此最终加速比是三者乘积。
+
+### 4.2 算子级：Kernel Fusion 三级递进 (贡献点 ①)
+
+#### 4.2.1 v1 —— compile-friendly 重构（消碎片）
+
+将 `skew/quat2dcm/quatmultiply` 等逐标量 `torch.tensor([[标量,...]])` 构造全部改写为 GPU 张量上的 `stack/cat` 操作，消除 `.item()` 同步和 1026 次/步的算子碎片。同时用"固定 3 维观测 + R_odo→1e12 抑制异常通道"替代动态观测维度分支。结果：`graph_count=1, graph_break=0`。
+
+**关键发现**：torch.compile(default) 反而更慢——超小图（241 op/步）的 codegen/守卫开销 > 融合收益。此负结果为后续手工 fusion 路线提供了动机。
+
+#### 4.2.2 v2 —— CUDA Graph Capture/Replay
+
+将单步捕获为 1 个 CUDA Graph，N 次 replay 消除 CPU 端 launch 调度开销 → 1.48× vs v0。踩坑：cuSOLVER 在 capture 中触发 host 同步 → 改用 3×3 解析 adjugate 逆；GPU 标量索引同步 → `index_select` + 1-elem idx。unroll 无效 → 证明瓶颈已从 launch-bound 转为 GPU-execution-bound。
+
+#### 4.2.3 v3 —— Triton 单 kernel Mega Kernel（核心）
+
+将整个 166,667 步串行 scan 写入 **1 个 Triton kernel**，531 微 kernel → **1 次 launch**。state（pos/vel/q/P, 16×16 padded）常驻寄存器，跨步零全局往返。矩阵全 padded-16 + `tl.dot`(fp64)；3×3 解析逆（无 cuSOLVER）；`libdevice.{atan2,sin,cos,sqrt,abs}`。
+
+**关键结果**：单条轨迹 fp64 **8.8×** vs CPU，vs 金标准偏差 ≤9µm。单步整步 ~60µs，比单次 cuSOLVER 3×3 求逆（134µs）还快——证明融合 + 解析逆 + 寄存器驻留在微矩阵场景碾压通用算子库。
+
+### 4.3 算法级：分层混合精度模型 (贡献点 ②)
+
+#### 4.3.1 三层精度需求分析
+
+EKF 各计算组件的精度需求差异达 3 个数量级：
+
+| 层 | 组件 | 精度需求 | 最优选择 | 依据 |
+|----|------|---------|---------|------|
+| Layer 1 | 传感器 I/O (gyro/accel/odom, pos/vel 输出) | ~1e-3 | **fp16** | 陀螺 0.02 rad/s, fp16 最小正规数 6e-8 → 裕度 >10⁶ |
+| Layer 2 | EKF 数学计算 (quat/SINS/H/F/z/S/K/x, P 协方差) | ~1e-5 | **fp32 必须** | q 归一化精度需求 >3 位小数；libdevice 函数固定返回 fp32 |
+| Layer 3 | 8 个 tl.dot (F@P@Fᵀ, H@P@Hᵀ, K@S 等) | 1e-8 | **ieee 最优** | TF32 对 16×16 矩阵 TensorCore 启动延迟无法摊销 |
+
+#### 4.3.2 实现：constexpr 模板参数
 
-三层优化架构：
-- 底层：系统级优化 → 仿真基础设施层（内存管理、数据流组织、计算-I/O 解耦）
-- 中层：算子级加速 → EKF 递推计算引擎层（矩阵构造与运算的微观级定制加速）
-- 顶层：算法级自适应 → 智能滤波策略层（信号理解与滤波质量提升）
+kernel body 与 v3 完全一致，仅将精度路径提为 `tl.constexpr` 模板参数：
+- `DT`: 内部计算精度（fp64/fp32）
+- `DT_IO`: 传感器 load 精度（fp16/fp32/bf16）
+- `IP`: `tl.dot` 的 `input_precision`（ieee/tf32）
 
-### 4.2 系统级优化（仿真基础设施层）
+constexpr 参数在 JIT 编译时特化，零运行时开销。
 
-#### 4.2.1 内存预分配与动态扩展消除
-- 针对障碍生成阶段的逐次数组拼接，建立全局预分配策略：预估障碍最大数量，一次性分配存储空间。
-- 对状态转移矩阵采用模板预构造，循环外一次性分配，循环内仅做增量更新。
-- 对轨迹数组、状态矩阵、中间变量统一预分配，消除运行时动态内存开销。
+#### 4.3.3 实测结果与关键发现
 
-#### 4.2.2 索引映射替代全序列搜索
-- 利用匀速运动的线性关系建立位置到时间索引的直接映射（O(1) 复杂度）。
-- 替代原有对全序列遍历搜索（O(N) 复杂度）。
-- 在高密度障碍场景（每 2-4m 一个）下展现显著扩展性优势。
+| 精度方案 | 吞吐 (步/s) | vs fp64 | RMSE (mm) | 判定 |
+|---------|------------|---------|----------|------|
+| fp64 (v3) | 17,090 | 1.0× | 基线 | 金标准对照 |
+| **fp32 (v4)** | **243,587** | **14.3×** | **ΔX≈0.11mm** | ✅ **全局最优** |
+| tf32 (v4) | 209,472 | 12.3× | ΔX≈4.7mm | ⚠️ 更慢更差 |
+| fp16-io (v6) | ~229,000 | 13.4× | ΔZ≈0.48mm | ✅ 带宽受限场景 |
+| bf16-io (v6) | 226,433 | 13.2× | Z 轴 96mm | ❌ 累积型运算失效 |
 
-#### 4.2.3 计算-可视化-I/O 三阶段解耦
-- 将耦合的数值计算、绘图渲染和文件读写重构为独立模块。
-- 支持性能测试时零绘图开销运行。
-- 为批量实验和参数扫描提供清洁的计算接口。
+**四个关键发现**：
 
-### 4.3 算子级加速（EKF 递推计算引擎层）
+1. **fp32 是"免费午餐"，且收益远超预期。**原预期 fp32 仅快 1.5-2×，实测 14.3×。根因：Hopper 的 fp64 ALU 吞吐远低于 fp32；串行 latency-bound 下，fp64 的 transcendental、双倍寄存器压力、指令延迟被逐项放大。
+2. **fp16 传感器 I/O 是"准免费午餐"。**精度损失 <0.5mm，远小于系统 RMSE (~1m) 的 2 个数量级；内存带宽减半，吞吐 +7%。
+3. **TF32 对 ≤16×16 矩阵是反模式。**TensorCore 启动延迟 (~5-10 cycles) + 截断转换 (2-3 cycles) > 计算节省 (~1 cycle)。盈亏平衡点 tile ~64。
+4. **bf16 不适合累积型运算（重要负结果）。**单次 bf16 截断误差 ~0.78%（7-bit 尾数），累积 166,667 步后 Z 轴漂移 96mm（放大 10⁵ 倍）。
 
-#### 4.3.1 状态转移矩阵模板化与增量更新
-- 设计预构造的 F 模板矩阵，每步仅更新 4 个依赖当前姿态/角速度的 3×3 子块：
-  - 速度方程中与比力相关的子块
-  - 速度方程中与加计零偏相关的子块
-  - 姿态方程中与角速度相关的子块
-  - 姿态方程中与陀螺零偏相关的子块
-- 减少约 70% 的矩阵赋值操作。
+### 4.4 系统级：多轨迹 Batch 并行 (贡献点 ③)
 
-#### 4.3.2 观测矩阵双模板切换
-- 针对正常工况（3×15 观测矩阵）和异常工况（2×15 观测矩阵）分别预构造固定模板。
-- 每步仅更新与当前航向角和四元数相关的少量元素。
-- 避免每步重复分配和完整赋值观测矩阵。
+#### 4.4.1 方法
 
-#### 4.3.3 高频函数内联与临时变量消除
-- 将四元数→DCM 转换改写为就地内联计算，消除函数调用开销。
-- 将四元数乘法改写为直接公式展开。
-- 将反对称矩阵构造改写为就地赋值。
-- 整体消除 16.6 万步中每步 4-5 次的函数调用开销和临时变量分配。
+单条轨迹的卡尔曼递归无法在时间维并行，但多条独立轨迹天然可并行。将 v4 的 kernel body 不变，仅指针加 `pid × batch_stride` 偏移，`grid=(B,)` 将轨迹维映射为 CUDA block。
 
-### 4.4 算法级自适应（智能滤波策略层）
+#### 4.4.2 三区扩展模型
 
-#### 4.4.1 基于新息序列的 Q/R 自适应调整
-- 传统方案使用固定的过程噪声 Q 和量测噪声 R，无法适应不同管段的实际噪声水平。
-- 引入基于新息（innovation）序列的在线自适应机制：通过滑动窗口统计新息协方差，实时调整 Q/R 比例。
-- 使滤波器在平稳段收紧、扰动段放松，提升整体定位精度。
+由 register 压力决定（110 regs/thread → occupancy_limit_registers = 4 block/SM）：
 
-#### 4.4.2 UD 分解替代标准协方差递推
-- 标准形式的协方差预测和更新在长序列递推中可能累积数值非对称和正定性丧失。
-- 采用 UD 分解（Upper-triangular + Diagonal）形式重写协方差预测和更新。
-- 保证协方差矩阵始终正定对称，同时将矩阵乘法转化为三角矩阵运算，兼顾数值稳定性与计算效率。
+```
+  ┌──────────────────┬────────────────────┬──────────────────────┐
+  │  I. 线性区        │  II. 次线性区       │  III. 饱和区          │
+  │  B = 1..78       │  B = 96..256       │  B = 312..400        │
+  │  每 SM 1 block    │  每 SM >1 block     │  每 SM ≥4 blocks     │
+  │  效率 99%         │  效率 84%→60%      │  效率 60%→35%        │
+  │  speedup ≈ B     │  资源竞争开始        │  Register 饱和        │
+  └──────────────────┴────────────────────┴──────────────────────┘
+```
 
-#### 4.4.3 里程计异常判别的自适应阈值
-- 传统方案使用固定阈值进行异常判别，缺乏对运行状态变化的适应性。
-- 引入基于历史位移差统计的自适应阈值机制（如 3σ 准则）。
-- 使系统能根据实际运行状态动态区分正常波动与真实打滑/空转，降低误判率。
+#### 4.4.3 实测结果
+
+| B | Steps/s | Speedup vs B=1 | 效率 |
+|---|---------|----------------|------|
+| 1 | 248,587 | 1.0× | 100% |
+| 16 | 3,665,196 | 16.0× | 100% |
+| 78 | 19,082,020 | 77.9× | 99% |
+| 128 | 26,489,103 | 140× | 83% |
+| 256 | 38,009,491 | 150× | 60% |
+| 312 | 46,290,270 | 186× | 60% |
+
+**关键发现**：B=78（=SM 数）是性价比最优点，完美线性（效率 99%）。B=312 是 register 饱和上限。各轨迹结果逐位一致（traj_spread=0），确认 block 间无串扰。Stream 多流对均匀 workload 无收益。
 
 ---
 
 ## 5. 实验设计与评估
 
-### 5.1 实验环境与对比基线
-- 计算平台：CPU、内存、操作系统等实验环境。
-- 基线版本：未优化的原始定位仿真程序。
-- 优化版本：三层优化全部应用后的版本。
-- 数据规模：500 m、100 Hz、166668 个采样点。
-- 基线定位精度参考：X 方向 RMSE 1113.11 mm，相对精度 0.24%；Y 方向 4.95 mm；Z 方向 8.06 mm。
-
-### 5.2 算子级 benchmark
-- 轨迹生成耗时对比（预分配 + 索引映射 vs 原始版本）。
-- EKF 单步耗时对比（模板化 + 内联 vs 原始版本）。
-- 协方差递推耗时对比（UD 分解 vs 标准形式）。
-
-### 5.3 定位仿真 pipeline 性能评估
-- 单次完整仿真总耗时对比。
-- 不同总行程下的扩展性：100 m、500 m、1000 m、2000 m。
-- 不同采样频率下的扩展性：50 Hz、100 Hz、200 Hz。
-- 不同障碍密度下的扩展性：每 2-4m / 每 20-40m。
-- 内存占用对比。
-
-### 5.4 精度验证
-- 对比优化前后 X/Y/Z 三轴 RMSE（确保不劣于基线）。
-- 对比闭合误差和相对定位精度。
-- 对比障碍段径向扰动还原效果。
-- 验证自适应 Q/R 相比固定参数的精度提升。
-- 验证自适应阈值相比固定阈值的误判率降低。
-- 验证 UD 分解相比标准递推的数值稳定性改善。
-
-### 5.5 消融实验
-
-| 实验配置 | 验证目标 |
-|----------|----------|
-| 仅系统级优化 | 预分配 + 索引映射 + 解耦的独立加速效果 |
-| 仅算子级优化 | F/H 模板化 + 函数内联的独立加速效果 |
-| 仅算法级优化 | 自适应 Q/R + UD 分解 + 自适应阈值的精度提升效果 |
-| 系统级 + 算子级 | 纯性能优化的综合加速效果 |
-| 三层全部应用 | 性能与精度的综合提升效果 |
+### 5.1 实验环境
+- GPU: NVIDIA Hopper sm_90, 78 SM, 150GB HBM
+- CPU: Intel Xeon, 单核执行基线
+- 软件: PyTorch 2.11.0 + CUDA 12.9 + Triton 3.6.0
+- 数据规模: 500 m, 100 Hz, N=166,667 步（完整轨迹）
+- 金标准: v0 CPU fp64 eager，RMSE X=1017.838 Y=16.835 Z=8.141 mm
 
----
+### 5.2 全版本吞吐对比
 
-## 6. 相关工作与结论
+| 版本 | 方法 | 设备/精度 | 吞吐 (步/s) | vs CPU | 正确性 |
+|------|------|----------|------------|--------|--------|
+| v0 | 原始 eager | CPU fp64 | 1,866 | 1.00× | 金标准 |
+| v2 | CUDA Graph | GPU fp64 | 2,640 | 1.41× | 逐位等价 |
+| v3 | Triton Mega | GPU fp64 | 16,491 | 8.8× | Δ≤9µm |
+| **v4** | **+ fp32** | **GPU fp32** | **243,587** | **130×** | **亚毫米** |
+| v4 | + tf32 | GPU tf32 | 209,472 | 112× | 亚厘米 |
+| v5 ×78 | + batch | GPU fp32 | 19,082,020 | 10,224× | 逐位一致 |
+| v5 ×256 | + batch | GPU fp32 | 38,009,491 | 20,370× | 逐位一致 |
 
-### 6.1 相关工作
+### 5.3 精度验证
+- fp32 vs fp64 golden: 全轴偏差亚毫米（ΔX≈0.11mm, ΔZ≈0.13mm）
+- fp16-io vs fp32: ΔZ≈0.48mm，远小于系统 RMSE
+- bf16-io 失效: Z 轴漂移 96mm，量化累积误差的放大效应
+- 多轨迹 block 间: traj_spread=0，零串扰
 
-#### 6.1.1 导航定位仿真加速
-- 机器人仿真中的递推计算与数值积分优化。
-- 多传感融合定位仿真的实时计算需求。
-- 大规模 Monte Carlo 仿真与参数扫描中的并行计算。
+### 5.4 消融实验：三轴各自的独立贡献量
 
-#### 6.1.2 科学计算程序优化
-- 数值计算中的向量化与内存预分配技术。
-- 小矩阵运算优化与批量矩阵计算。
-- 计算密集型循环的算子融合与函数内联策略。
+| 实验配置 | 加速 vs v0 CPU | 验证目标 |
+|----------|---------------|----------|
+| 仅 Fusion (v3 fp64) | 8.8× | 轴① 独立贡献 |
+| 仅 Precision (v4 fp32) | 130× (= 8.8× × 14.3×)* | 轴② 增量贡献 |
+| 仅 Batch (v5 B=78) | 10,224× (= 130× × 77.9×)* | 轴③ 增量贡献 |
 
-#### 6.1.3 自适应滤波与数值稳定性
-- 基于新息的自适应 Kalman 滤波方法。
-- UD 分解与平方根滤波器的数值稳定性改进。
-- 自适应阈值在故障检测与隔离中的应用。
+*注：各轴加速因子的乘积性，已在全版本序列中自然验证。
 
-### 6.2 总结
-- 总结本文针对套管井机器人融合定位仿真提出的"稳-快-准"三层递进优化方法。
-- 系统级优化消除内存抖动和冗余搜索，打牢仿真基础。
-- 算子级加速实现大规模 EKF 循环体瘦身，单步开销最小化。
-- 算法级自适应让滤波器理解信号特性，提升定位精度与鲁棒性。
+### 5.5 SM 利用率验证 (ncu)
 
-### 6.3 未来工作
-- 高性能语言实现进一步加速。
-- GPU 批量 EKF 并行化。
-- 真实传感器数据验证。
-- 频域异常检测与振动信号分析。
-- 实时嵌入式部署。
+| 配置 | sm__throughput | warps_active |
+|------|---------------|-------------|
+| 单 block (v4) | 0.27% | 6.25% (4/64 warp) |
+| B=78 (v5) | 19.91% (~74×) | — |
 
----
+### 5.6 算子库微基准对比
 
-## 章节对应关系（参照 ASTRA）
+| 算子 | 耗时 | 说明 |
+|------|------|------|
+| cuBLAS GEMM (F@P@Fᵀ) | 17.5 µs | 单次 15×15 矩阵乘 |
+| cuSOLVER inv (3×3) | 134 µs | 通用库固定开销主导 |
+| **v3 整步 (全部)** | **~60 µs** | 融合后整步比单次 cuSOLVER 还快 |
 
-| ASTRA 章节 | 本文对应章节 | 内容 |
-|------------|-------------|------|
-| §1 Introduction | §1 引言 | 套管井定位背景 + 仿真性能瓶颈 + 三层贡献列表 |
-| §2 Background | §2 背景与计算模式 | 定位系统方案 + 仿真数据集 + 计算模式分类 |
-| §3 Motivation | §3 性能瓶颈分析与优化动机 | profiler 实测 + 瓶颈可视化 + 优化目标 |
-| §4 Methodology | §4 优化方法 | 三层优化的具体设计与实现 |
-| §5 Evaluation | §5 实验设计与评估 | 性能对比 + 精度验证 + 消融实验 |
-| §6 Related Work & Conclusion | §6 相关工作与结论 | 导航仿真加速 + 计算优化 + 自适应滤波 + 总结展望 |
+## 6. 相关工作与结论
 
----
+### 6.1 相关工作
 
-## 与 ASTRA 的类比映射
+#### 6.1.1 GPU 加速递归状态估计
+- GPU 卡尔曼滤波库几乎全部针对 batched 多轨迹场景设计，**缺乏单轨迹串行 latency-bound 的高性能实现**——本工作的核心空白点
+- Särkkä & García-Fernández (IEEE TAC 2021): 贝叶斯平滑器的 associative scan 并行化，O(N)→O(log N)，与本工作正交的算法级并行方向
+- Mamba/FlashAttention 系列的 Triton 单 kernel 融合范式：本文借鉴其 IO-aware fusion 思想
 
-| ASTRA 技术 | 本文对应技术 | 共同思想 |
-|------------|-------------|----------|
-| GPU 内存池预分配 | 数组预分配 + F/H 模板 | 消除运行时动态分配 |
-| 紧凑张量存储格式 | 位置-索引直接映射 | 用轻量计算替代昂贵的数据搜索 |
-| CUDA 多流重叠 | 计算-可视化-I/O 解耦 | 将独立阶段分离以消除阻塞 |
-| ADMM Fused Kernel | EKF 函数内联 + 模板化 | 减少函数调用和中间变量 |
-| Tensor Core 混合精度 | UD 分解三角化 | 用更高效的数学形式替代通用矩阵乘 |
-| 相似性感知跳过 | 新息自适应 Q/R | 利用信号特性减少不必要的计算/调整 |
-| 输入感知自动调参 | 自适应异常阈值 | 根据实时数据特征动态调整参数 |
+#### 6.1.2 混合精度在科学计算中的应用
+- 深度学习 AMP (Automatic Mixed Precision): 解决吞吐受限 + 大量并行 MAC vs 本文解决 latency-bound + 串行递归——问题本质不同
+- 传统观点认为 EKF 需 fp64 保证数值稳定性——本文量化证明 fp32 在 mm 级定位中完全足够
+- TF32/bf16 的负结果为混合精度在递归滤波中的适用边界提供了量化证据
 
----
+#### 6.1.3 GPU 对 latency-bound 负载的优化
+- CUDA Graph: 消除 CPU 端调度开销，但无法消除 GPU 内 kernel 间转换延迟
+- Persistent Kernel: 本文 v3/v5 的单次 launch 已是持久化 kernel 的变体
+- Warp Specialization / TMA / TensorCore: 经分析（§7 负结果 / FINAL_REPORT §7）为结构性错配，不适用于微矩阵串行递归
+
+### 6.2 总结
+- 本文提出面向 SINS/双里程计/EKF 融合定位仿真的三轴乘积式 GPU 加速框架
+- 算子级：531 kernel → 1 Triton mega-kernel，加速 8.8×
+- 算法级：三层精度模型，fp32 最优，加速 14.3×（fp32 vs fp64）
+- 系统级：多轨迹 batch 并行，三区扩展模型，B≤78 完美线性
+- 三轴乘积 ≈ 18,000× 聚合加速，定位精度保持亚毫米
+
+### 6.3 未来工作
+- Associative-scan 并行 EKF：打破单轨迹串行本质，O(N)→O(log N)
+- F 块稀疏优化：降低寄存器压力，推高 batch 饱和点
+- 持久化 kernel + 在线流式：100Hz 实时场景下的常驻 kernel 方案
+- 多 GPU 扩展：跨 GPU 的轨迹级并行
 
-## 层次总结概览
+---
 
-| 层次 | 核心技术 | 价值 | 对应 ASTRA 层次 |
-|------|----------|------|----------------|
-| 1. 系统级 | 预分配、索引映射、三阶段解耦 | "稳"：消除内存抖动和冗余搜索，打牢仿真基础 | 内存池 + 紧凑格式 + 流水线 |
-| 2. 算子级 | F/H 模板化、函数内联、增量更新 | "快"：大规模循环体极致瘦身，单步开销最小化 | MTTKRP/ADMM/HIST 定制内核 |
-| 3. 算法级 | Q/R 自适应、UD 分解、自适应阈值 | "准"：让滤波器理解信号，提升精度与鲁棒性 | 相似性跳过 + 自动调参 |
+## 论文结构总览
+
+| 章节 | 内容 | 对应贡献点 |
+|------|------|-----------|
+| §1 Introduction | 套管井定位背景 + GPS 拒止导航共性（深空→地下→具身智能）+ GPU 不友好性量化 + 三贡献列表 | 全部 |
+| §2 Background & Problem Analysis | SINS/EKF 算法流程 + 小矩阵串行递归计算模式 + Profiling 证据 | — |
+| §3 Kernel Fusion | v1 消碎片 → v2 CUDA Graph → v3 Triton Mega Kernel + 寄存器驻留设计 | ① |
+| §4 Heterogeneous Precision | 三层精度需求模型 + fp32/fp16/TF32/bf16 系统对比 + 反直觉负结果 + 泛化设计准则 | ② |
+| §5 Multi-Trajectory Parallelism | 三区扩展模型 + B 扫描 + Register 压力分析 + SM 利用率验证 | ③ |
+| §6 Evaluation | 全版本吞吐 + 精度验证 + 消融实验（各轴独立贡献） + 算子库对比 | 全部 |
+| §7 Discussion & Generalization | 三轴框架的泛化适用性 + 负结果方法论价值 + 同类问题的迁移路径 | — |
+| §8 Related Work | GPU KF 库 + SSM parallel scan + 混合精度科学计算 + latency-bound 优化 | — |
+| §9 Conclusion | 三轴乘积式框架总结 + 未来方向 | — |
+
+## 三贡献点层次总结
+
+| 层次 | 核心技术 | 价值 | 版本 |
+|------|----------|------|------|
+| 1. 算子级 (Fusion) | 531 kernel → 1 Triton mega-kernel，state 寄存器驻留 | "消碎片"：消除 launch 链延迟，单条 8.8× | v1→v2→v3 |
+| 2. 算法级 (Precision) | 三层精度需求模型，fp32 全局最优，fp16 I/O 准免费 | "不浪费"：匹配硬件 ALU 吞吐，增量 14.3× | v4, v6 |
+| 3. 系统级 (Parallelism) | 三区扩展模型，B≤78 完美线性，register 压力决定饱和点 | "填满 GPU"：多轨迹 batch 填满 78 SM，增量 ≤78× | v5, v7 |
diff --git "a/\350\256\272\346\226\207\345\256\217\345\244\247\350\203\214\346\231\257\345\210\206\346\236\220.md" "b/\350\256\272\346\226\207\345\256\217\345\244\247\350\203\214\346\231\257\345\210\206\346\236\220.md"
index adfdb06..429ff20 100644
--- "a/\350\256\272\346\226\207\345\256\217\345\244\247\350\203\214\346\231\257\345\210\206\346\236\220.md"
+++ "b/\350\256\272\346\226\207\345\256\217\345\244\247\350\203\214\346\231\257\345\210\206\346\236\220.md"
@@ -1,6 +1,8 @@
 # 论文宏大背景分析：从深空到深地，在具身智能时代重新审视多传感融合状态估计的计算底座
 
 > 本文档为论文 Introduction 的宏大背景提供素材与分析框架。核心论点：GPS 拒止环境下的"状态估计"是天上、地下、具身智能三类场景的共同计算底座，而当前 GPU 对此类递归小矩阵负载极度不友好——这正是本文的切入点。
+>
+> **目标期刊：Frontiers of Computer Science (FCS)**。FCS 为 Springer/Higher Education Press 联合出版的英文计算机科学综合期刊，读者覆盖计算机科学全领域（不限于 HPC/体系结构）。因此 Introduction 需兼顾：① 对非 HPC 读者的 GPU 基本概念铺垫；② 对非机器人读者的定位问题背景解释；③ 让 HPC 审稿人看到技术深度的同时，让 general CS 审稿人看到问题的广泛相关性。
 
 ---
 
@@ -197,24 +199,25 @@
 
 ## 四、论文标题的备选方案
 
-这些标题试图将 "天上-地下-具身智能" 的宏大叙事压缩到一行，并保留具体的技术贡献标识：
+以下标题面向 FCS 综合期刊读者，需兼顾技术深度与跨领域可读性：
 
 | 编号 | 标题 | 风格 |
 |------|------|------|
-| A | **From the Moon to the Mine: GPU Acceleration of Recursive State Estimation for GPS-Denied Autonomous Systems** | 诗意 + 具体场景，"Moon-Mine" 对称 |
-| B | **Taming the Latency-Bound Beast: GPU-Centric Acceleration of EKF Fusion Positioning via Kernel Fusion × Mixed Precision × Batch Parallelism** | 技术先行，以"驯服怪兽"为钩子，副标题给出技术方案 |
-| C | **GPS-Denied, GPU-Hostile: Accelerating Recursive Bayesian Filtering for Multi-Sensor Fusion from Deep Space to Embodied AI** | 挑战-方案型，拉通三个领域 |
-| D | **When Kalman Meets CUDA: A Latency-Bound Study of GPU-Accelerated Sequential State Estimation and Its Multiplicative Optimization Framework** | 学术型，"Kalman 遇上 CUDA" 是可记忆的短语 |
-| E | **Revisiting the Computational Base of GPS-Denied State Estimation: Why GPU Hates Recursive Filtering and How to Fix It** | 问题导向，强调"computational base" |
+| A | **From Deep Space to Deep Underground: GPU-Accelerated Recursive State Estimation for GPS-Denied Multi-Sensor Fusion** | 宏大叙事，三个领域拉通 |
+| B | **Taming Latency-Bound Recursive Filtering on GPUs: A Multiplicative Optimization Framework via Kernel Fusion, Heterogeneous Precision, and Multi-Trajectory Parallelism** | 技术先行，适合 HPC 方向审稿人 |
+| C | **Why GPU Hates Recursive Filtering: Profiling, Diagnosis, and a Three-Axis Acceleration Framework for Sequential State Estimation** | 问题导向，"why-hate" 钩子吸引非 HPC 读者 |
+| D | **When Kalman Meets CUDA: A Latency-Bound Study of GPU-Accelerated EKF Fusion Positioning** | 学术型，"Kalman 遇上 CUDA" 跨领域识别度高 |
+| E | **Accelerating Multi-Sensor Fusion Positioning on GPUs: Kernel Fusion, Mixed Precision, and Batch Parallelism for Recursive Bayesian Filtering** | 直接描述型，审稿人一眼看懂内容 |
 
-**推荐**：A 做中文语境的主标题（诗意），B 做副标题（技术细节）。组合：
+**推荐**：
 
-> **From the Moon to the Mine: GPU Acceleration of Recursive State Estimation for GPS-Denied Autonomous Systems**
-> *Taming the Latency-Bound Beast via Kernel Fusion, Mixed Precision, and Multi-Trajectory Parallelism*
+对于 FCS 综合期刊，推荐 **C 为主标题**（"Why GPU Hates" 作为 hook 能吸引非 HPC 背景的审稿人）+ **B 的副标题**：
 
-或者更直接的学术风格：
+> **Why GPU Hates Recursive Filtering: A Multiplicative Acceleration Framework via Kernel Fusion, Heterogeneous Precision, and Multi-Trajectory Parallelism**
 
-> **When Kalman Meets CUDA: Why GPU is 4× Slower than CPU for Recursive Fusion Positioning and How Kernel Fusion × Mixed Precision × Multi-Trajectory Parallelism Deliver 18,000× Speedup**
+或者更偏向应用的 **A**（"Deep Space to Deep Underground" 强调了问题的广泛性，符合 FCS 读者的多样性）：
+
+> **From Deep Space to Deep Underground: GPU-Accelerated Recursive State Estimation via a Three-Axis Multiplicative Optimization Framework**
 
 ---
 
@@ -231,11 +234,16 @@
 
 ### 5.2 审稿人可能的质疑及预判
 
-| 质疑 | 预判回答 |
-|------|---------|
-| "深空探测和井下机器人的传感器配置完全不同，EKF 的维度也不同，你怎么论证'通用'？" | 我们不声称算法通用——我们强调**计算形式通用**。无论状态是 6 维（航天器交会对接）还是 15 维（本文 SINS/EKF）还是 27 维（双目 VIO EKF），它们都是**同一类递归矩阵运算**：$F_k \cdot P_{k-1} \cdot F_k^T$、$P_k H_k^T (H_k P_k H_k^T + R_k)^{-1}$、$(I - K_k H_k) P_k$。我们的加速框架（fusion × precision × batch parallelism）对所有此类运算都适用。 |
-| "你只在 15 维 166k 步的 SINS/EKF 上验证了，怎么能说对深空探测有用？" | 我们的贡献是**方法 + 方法论**。方法是具体的 kernel fusion 技术（v1-v5），方法论是对"latency-bound 微矩阵递归负载"的 GPU 加速空间的结构性分析（§3.5 的负结果研究）。方法论的结论——"不要堆 TC/TMA/warp-specialization，要走 fusion + precision + batch parallelism"——对任何同类 workload 都适用。我们欢迎在更大规模/不同维度的系统上验证。 |
-| "具身智能用因子图/SLAM 而不是 EKF，你怎么论证 EKF 的相关性？" | 两个回答：(1) 即便整体系统使用因子图优化（全局），前端仍然需要 EKF 做高频状态传播（IMU 预积分），因为 IMU 的频率（100-1000Hz）远超因子图优化频率（10-30Hz）；(2) 边缘部署的趋势是**轻量化**，EKF 作为计算最优的递归滤波器在这个趋势下不仅没有过时，反而在强化。 |
+**FCS 特有的审稿人画像**：约 1/3 HPC/体系结构方向，1/3 AI/机器人方向，1/3 其他 CS 方向。审稿意见可能来自截然不同的视角。
+
+| 质疑 | 来源方向 | 预判回答 |
+|------|---------|---------|
+| "深空探测和井下机器人的传感器配置完全不同，EKF 的维度也不同，你怎么论证'通用'？" | 机器人/AI | 我们不声称算法通用——我们强调**计算形式通用**。无论状态是 6 维（航天器交会对接）还是 15 维（本文 SINS/EKF）还是 27 维（双目 VIO EKF），它们都是**同一类递归矩阵运算**。我们的加速框架（fusion × precision × batch parallelism）对所有此类运算都适用。另见 §泛化讨论中的"适用条件"形式化刻画。 |
+| "你只在 15 维 166k 步的 SINS/EKF 上验证了，怎么能说对深空探测有用？" | HPC/系统 | 我们的贡献是**方法 + 方法论**。方法是具体的 kernel fusion 技术（v1-v5），方法论是对"latency-bound 微矩阵递归负载"的 GPU 加速空间的结构性分析。方法论的结论对任何同类 workload 都适用。我们明确列出了泛化边界（矩阵维数 ≤30, 步数 ≥10⁴）和不适用的场景。 |
+| "具身智能用因子图/SLAM 而不是 EKF，你怎么论证 EKF 的相关性？" | AI/机器人 | 两个回答：(1) 即便整体系统使用因子图优化（全局），前端仍然需要 EKF 做高频状态传播（IMU 预积分），因为 IMU 的频率（100-1000Hz）远超因子图优化频率（10-30Hz）；(2) 边缘部署的趋势是**轻量化**，EKF 作为计算最优的递归滤波器在这个趋势下不仅没有过时，反而在强化。 |
+| "fp32 换 fp64 算什么贡献？不就是改了数据类型吗？" | HPC/系统 | 核心贡献不是"换精度"本身，而是：① **三层精度需求模型的建立**——首次量化了递归滤波中各组件的精度需求差异（跨 3 个数量级）；② **系统性对比实验**——fp32/fp16/TF32/bf16 四方案的精度-吞吐 Pareto 分析；③ **两个反直觉负结果的深入分析**——TF32 对 ≤16×16 矩阵为反模式（TC 启动延迟无法摊销），bf16 在 10⁵ 步累积后发散（7-bit 尾数的截断误差放大 10⁵ 倍）。这些发现对同类递归滤波的 GPU 实现有直接的指导意义。 |
+| "你的 18,000× 加速比有误导性——包含了 batch 并行的贡献，单轨迹没有那么快。" | 通用 | 我们在 §1 Introduction 的贡献列表中**明确区分**了三个加速维度：单轨迹 fusion 8.8×、单轨迹 precision 14.3×、多轨迹 parallelism ≤78×。18,000× 是"三轴乘积"的理论上限，实验部分分别报告了各轴独立加速比和消融结果，不会给读者造成误导。 |
+| "论文太长了/太工程了，不适合 FCS。" | 通用/编辑 | FCS 接受 full-length research article（通常 15-25 页），本文的深度和篇幅在此范围内。我们强调**方法论贡献**（三轴框架的泛化性、精度模型的系统性、负结果的方法论价值）而非单纯的工程报告。论文结构上，将 §7 Discussion 专门用于泛化讨论和方法论提炼。 |
 
 ### 5.3 如何处理"深空探测"的引用
 
@@ -324,7 +332,9 @@ naive GPU execution of this workload is 4× slower than a single CPU core.
 
 | 维度 | 结论 |
 |------|------|
+| **目标期刊** | **Frontiers of Computer Science (FCS)** — 英文综合 CS 期刊，读者覆盖全 CS 领域 |
 | **"宏大"的本质** | 不只是"我的场景很厉害"，而是"我的场景+深空探测+DARPA SubT+具身智能共同指向同一个计算形式的性能瓶颈" |
-| **叙事逻辑** | 天上（无 GPS）→ 地下（无 GPS + 干扰）→ 具身智能（无 GPS + 边缘部署）→ 共同底座（递归贝叶斯滤波）→ 这个底座在 GPU 上有系统性性能问题 → 本文解决它 |
-| **本文定位** | 不解决"用什么传感器"或"融合什么算法"——各领域已有答案。解决**"这个融合计算怎么跑得快"**——它是最被忽视的"计算底座"层 |
-| **标题建议** | "From the Moon to the Mine: ..." 或 "When Kalman Meets CUDA: ..." |
\ No newline at end of file
+| **叙事逻辑** | 天上（无 GPS）→ 地下（无 GPS + 干扰）→ 具身智能（无 GPS + 边缘部署）→ 共同底座（递归贝叶斯滤波）→ 这个底座在 GPU 上有系统性性能问题 → 本文提出三轴乘积式解决方案 |
+| **本文定位** | 不解决"用什么传感器"或"融合什么算法"——各领域已有答案。解决**"这个融合计算怎么跑得快"**——它是被忽视的"计算底座"层 |
+| **FCS 适配策略** | ① Introduction 需对非 HPC 读者铺垫 GPU 基本概念；② 对非机器人读者解释定位问题背景；③ 设 §7 Discussion 专门讨论泛化适用性；④ 方法论提炼（三轴框架的适用范围 + 负结果的设计准则）提升论文的跨领域价值 |
+| **推荐标题** | **"Why GPU Hates Recursive Filtering: A Multiplicative Acceleration Framework via Kernel Fusion, Heterogeneous Precision, and Multi-Trajectory Parallelism"** 或 **"From Deep Space to Deep Underground: GPU-Accelerated Recursive State Estimation via a Three-Axis Multiplicative Optimization Framework"** |
\ No newline at end of file
