# 混合精度方案简述

## 1. 设计目标

EKF 各部分的数值敏感性不同，因此不能简单地将整个计算过程统一降为低精度。本方案采用**组件级混合精度**：对精度要求较低的传感器输入降低存储精度，对容易产生累计误差的状态递推和协方差计算保留 fp32。

## 2. 精度配置

| 计算部分 | 推荐精度 | 原因 |
|---|---|---|
| 陀螺仪、加速度计、里程计输入 | fp16 存储，读取后转 fp32 | 减少数据读取量，输入范围适合 fp16 |
| 位置、速度、四元数 | fp32 | 防止毫米级修正和姿态微小变化被舍入 |
| EKF 状态与协方差计算 | fp32 | 控制长序列递推误差，保持数值稳定性 |
| 16x16 小矩阵乘法 | IEEE fp32 | TF32 在微矩阵上的转换和启动成本较高 |

最终配置为：

```text
传感器输入：fp16
状态与协方差计算：fp32
矩阵乘法：IEEE fp32
```

## 3. 实验结论

在 `N=5000`，即 5000 个采样点的快速实验中，推荐方案的吞吐由约 `6562 steps/s` 提升至 `6995 steps/s`，提升约 7%；Z 轴 RMSE 额外增加约 `0.48 mm`，仍处于毫米级范围。

其他低精度方案未获得收益：

- bf16 尾数精度不足，微小传感器差分误差在长序列递推中被放大，导致明显漂移。
- 将位置、速度或四元数降为 fp16 会丢失小幅状态修正，造成较大定位误差。
- TF32 面向大矩阵吞吐优化，但本文矩阵最大约为 16x16，其额外转换和启动成本超过计算收益，实测性能反而下降。

## 4. 方案总结

本方案的创新点不是简单使用 fp16，而是根据 EKF 的数值结构进行精度分配：在传感器输入边界降低精度，在递归状态和协方差计算中保留 fp32。该方法在基本保持定位精度的同时减少输入数据开销，并避免 bf16 和 TF32 在小矩阵、长序列递推场景中的负收益。

正式论文应以完整数据集 `N=166667` 的重复实验作为主要结果，并补充运行时间方差、显存带宽和全轨迹定位误差。
